generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model KeyHolder {
  discordServer String @default("")
  discordUserId String @default("")
  discordRoleId String @default("")
  address       String @id @default("")
  ftAddress     String @default("")
}
